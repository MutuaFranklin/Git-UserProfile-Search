import { ImageTransformDirective } from './image-transform.directive';

describe('ImageTransformDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageTransformDirective();
    expect(directive).toBeTruthy();
  });
});
